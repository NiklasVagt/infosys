generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"
}

model InfosysEvent {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  author      String
  date        DateTime
}
