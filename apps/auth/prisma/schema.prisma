generator client {
  provider = "prisma-client-js"
  output   = "../../../node_modules/@infosys/auth"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db.sql"
}

model User {
  id       Int    @id @default(autoincrement())
  username String @unique
  password String
}
